import { PermissionAllPipe } from './permission-all.pipe';

describe('PermissionAllPipe', () => {
  it('create an instance', () => {
    const pipe = new PermissionAllPipe();
    expect(pipe).toBeTruthy();
  });
});
