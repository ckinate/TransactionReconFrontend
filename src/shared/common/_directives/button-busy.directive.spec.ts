import { ButtonBusyDirective } from './button-busy.directive';

describe('ButtonBusyDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonBusyDirective();
    expect(directive).toBeTruthy();
  });
});
