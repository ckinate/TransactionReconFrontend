import { BusyIfDirective } from './busy-if.directive';

describe('BusyIfDirective', () => {
  it('should create an instance', () => {
    const directive = new BusyIfDirective();
    expect(directive).toBeTruthy();
  });
});
